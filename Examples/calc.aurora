Clinical:
NGC1: @10:30
// Spaces are optional after hypen now
@2023/11/16 10:30 CC1 - 'Nar1'

Hb[13.5 mg, 12.8 mg] -- 'Normal range for adult female.';

arith_module binaryexpressions {
    3 * 6;
    2 + (1 + 1);
    4 / 1;
    (4 + 2) / 3 * 2;
    10 - 2 * 5 / 3;
    4 ^ 2;
    8 % 2;
    6 - 1;
    1 - 5;
    (5 + 1) - 7 * (1 - 3);
}